# Core dependencies
flask==3.1.2
python-dotenv==1.1.1

# Optional but useful (recommended for future enhancements)
flask-cors==5.0.0       # For enabling frontend-backend communication
sqlalchemy==2.0.36       # ORM for database operations (optional but powerful)
werkzeug==3.1.3          # Flask's core dependency, ensures version consistency
jinja2==3.1.6            # Template rendering engine
itsdangerous==2.2.0      # Security for session cookies
blinker==1.9.0           # For Flask signals and extensions
click==8.3.0             # Command-line interface for Flask commands

# === Core Dependencies ===
Flask==3.1.2
python-dotenv==1.1.1

# === Optional (for future improvements) ===
Flask-Cors==5.0.0          # Allow cross-origin requests (useful if you add a frontend)
Flask-RESTful==0.3.10      # Structured REST API routing
Jinja2==3.1.6              # HTML templating
Werkzeug==3.1.3            # Flask internal dependency
itsdangerous==2.2.0        # Flask internal dependency
click==8.3.0               # Command line utilities
